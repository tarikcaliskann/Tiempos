import 'package:flutter/material.dart';
import 'package:google_sign_in_web/web_only.dart' as gsi;

/// Google Identity Services button (web only).
Widget googleSignInGisButton() => gsi.renderButton();
